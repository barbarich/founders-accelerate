import M6PersonCard from "./M6PersonCard";
import { POST_AUTH_FLOW_PROMPT } from "./promptTemplates";

export default function M6Slide09Vlad() {
  return (
    <M6PersonCard
      order={3}
      name="Vlad"
      product="Выбор идеи + каркас продукта"
      state="Сначала выбираешь ОДНУ идею и защищаешь перед группой. Дальше — каркас целого продукта + первый флоу. Без рабочего каркаса к концу встречи = идея не прошла."
      task="Выбрать идею → собрать каркас продукта + первый флоу"
      taskDetail="3 вопроса вслух (кому конкретно / какое ОДНО действие / что он получает обратно). Проходишь — заполняешь шаблон справа детально под свою идею, собираешь в Lovable не один экран, а целый каркас с первым флоу."
      promptTitle="Шаблон post-auth flow (каркас + первый флоу)"
      promptBody={POST_AUTH_FLOW_PROMPT}
      criterion="Публичный URL с логином. Навигация из 3+ разделов, работающий первый флоу, вторичные разделы с mock-контентом. 3 вопроса по выбору идеи прошли проверку группы. Чеклист раздела 8 пройден."
    />
  );
}
