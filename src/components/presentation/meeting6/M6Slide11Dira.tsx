import M6PersonCard from "./M6PersonCard";
import { CHAT_BOT_FLOW_PROMPT } from "./promptTemplates";

export default function M6Slide11Dira() {
  return (
    <M6PersonCard
      order={5}
      name="Inna + Aleksandra"
      product="Dira.click"
      state="WhatsApp-бот для поиска жилья. Сегодня — прогоняем текущий флоу через ИИ как через QA, дорабатываем онбординг и доводим первую выдачу до Aha-момента."
      task="Провалидировать флоу с ИИ → доработать онбординг и Aha"
      taskDetail="Прогон: даёшь Claude/GPT роль типичного клиента (из контекста раздела 1 шаблона) и прогоняешь текущий флоу — где он теряется, что непонятно, где раздражает. По результатам — точечная переделка первых сообщений и того, что превращает первую выдачу в «это работает для меня». Шаблон справа — каркас бота + детальный первый флоу + вторичные флоу (/saved, /alerts, /help)."
      promptTitle="Шаблон chat-bot flow (каркас + первый флоу)"
      promptBody={CHAT_BOT_FLOW_PROMPT}
      criterion="Ссылка/QR на обновлённый бот + транскрипт прогона через ИИ (роль клиента) с зафиксированными точками трения и их исправлениями. 3 новых клиента прошли флоу end-to-end и зафиксировали Aha-момент словами. Чеклист раздела 8 пройден."
    />
  );
}
